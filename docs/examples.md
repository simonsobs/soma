# Examples

These notebooks are shown with the outputs they were saved with. The runnable copies,
and the Python scripts that generate them, are in the `examples/` directory of the
repository.

```{toctree}
:maxdepth: 1
:glob:

examples/*
```
